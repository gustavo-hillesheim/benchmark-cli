scripts:
    benchmark: deno run --allow-env --allow-read --allow-run cli.ts
    install: deno install --allow-env --allow-read --allow-run -f cli.ts
    compile: deno compile --unstable --allow-env --allow-read --allow-run cli.ts
